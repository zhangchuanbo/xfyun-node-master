README for Linux_aisound
-----------------------

bin：
|-- msc
    |-- msc.cfg（作用：msc调试、生成msc日志）
	|-- res
		|--tts（合成资源文件）
		
doc：
|-- readme.txt

include：调用SDK所需头文件

libs：
|-- x86
	|-- libmsc.so（32位动态库）
|-- x64
	|-- libmsc.so（64位动态库）

